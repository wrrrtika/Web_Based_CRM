<!Doctype HTML>
<html>
<body>
<header></header>


<h1>All Contacts</h1>

<ul>
<% @contacts.each do |contact| %>
  <li><%= contact.first_name %> <%= contact.last_name %> <%= contact.email %> <%= contact.note %> </li>
<% end %>
</ul>

</body>
</html>